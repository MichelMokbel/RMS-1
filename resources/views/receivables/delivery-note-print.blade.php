<!doctype html><html lang="en"><head><meta charset="utf-8"><title>{{ __('Delivery Note') }} {{ $deliveryNote->delivery_note_number }}</title><style>@page{size:A4;margin:14mm}*{box-sizing:border-box}body{font-family:Arial,sans-serif;color:#111;margin:0}.no-print{margin-bottom:12px}.btn{padding:7px 12px;border:1px solid #bbb;border-radius:6px;background:#fff}.header{text-align:center;border-bottom:2px solid #111;padding-bottom:12px}.header h1{margin:0;font-size:24px}.meta{display:grid;grid-template-columns:1fr 1fr;gap:8px 24px;margin:20px 0;font-size:13px}.label{font-weight:700}.address{white-space:pre-line}.items{width:100%;border-collapse:collapse}.items th,.items td{border:1px solid #aaa;padding:8px}.items th{background:#f3f4f6;text-align:left}.qty{text-align:right}.notes{margin-top:18px;white-space:pre-line}.signatures{display:grid;grid-template-columns:1fr 1fr;gap:50px;margin-top:60px}.signature{border-top:1px solid #111;padding-top:6px;text-align:center;font-size:12px}@media print{.no-print{display:none}}</style></head><body><div class="no-print"><button class="btn" onclick="window.print()">{{ __('Print') }}</button></div><div class="header"><h1>{{ __('DELIVERY NOTE') }}</h1><div>{{ $deliveryNote->delivery_note_number }}</div></div><div class="meta"><div><span class="label">{{ __('Customer') }}:</span> {{ $deliveryNote->customer_name_snapshot }}</div><div><span class="label">{{ __('Date') }}:</span> {{ $deliveryNote->delivery_date->format('Y-m-d') }}</div><div><span class="label">{{ __('Reference') }}:</span> {{ $deliveryNote->reference ?: '—' }}</div><div><span class="label">{{ __('Branch') }}:</span> {{ $deliveryNote->branch_id }}</div><div style="grid-column:1/-1"><span class="label">{{ __('Delivery Address') }}:</span><div class="address">{{ $deliveryNote->delivery_address_snapshot ?: '—' }}</div></div></div><table class="items"><thead><tr><th style="width:40px">#</th><th>{{ __('Description') }}</th><th style="width:90px">{{ __('Quantity') }}</th><th style="width:90px">{{ __('Unit') }}</th><th>{{ __('Notes') }}</th></tr></thead><tbody>@foreach($deliveryNote->items as $index => $item)<tr><td>{{ $index+1 }}</td><td>{{ $item->description }}</td><td class="qty">{{ rtrim(rtrim(number_format((float)$item->qty,3,'.',''),'0'),'.') }}</td><td>{{ $item->unit }}</td><td>{{ $item->line_notes }}</td></tr>@endforeach</tbody></table>@if($deliveryNote->notes)<div class="notes"><strong>{{ __('Notes') }}:</strong><br>{{ $deliveryNote->notes }}</div>@endif<div class="signatures"><div class="signature">{{ __('Prepared By') }}</div><div class="signature">{{ __('Received By / Signature') }}</div></div></body></html>
